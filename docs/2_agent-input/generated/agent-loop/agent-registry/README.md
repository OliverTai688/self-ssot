# AgentFacts-Lite Internal Registry

Generated by loop 27 for `AGENT-005`.

This workspace is internal-only agent-loop evidence. It is not a public registry, a runtime endpoint, a database table, or a NANDA Index registration.

## Files

- `internal-agent-manifests.agentfacts-lite.json`: AgentFacts-lite manifests for every internal governance agent in `docs/02_architecture-and-rules/ARC-020_internal-agents.md`.
- `manifest-index.json`: lightweight coverage index for the manifest inventory.

## Validation

Run this before editing or depending on the generated registry:

```bash
pnpm agent:registry:check
pnpm agent:registry:check -- --json
pnpm agent:registry:check -- --out docs/2_agent-input/generated/agent-loop/reports/<file>.json
```

The checker validates required AgentFacts-lite fields, coverage against `ARC-020`, local source file references, high-risk capability approval gates, no-secret markers, and the internal-only registry posture.

## Safety State

- `externalRegisterable` is `false` for every agent.
- `registrationStatus` is `not-registered` for every agent.
- `endpoints.internal` and `endpoints.external` are empty until a reviewed runtime registry surface exists.
- No secrets, tokens, cookies, database URLs, profile IDs, private records, telemetry metrics, certifications, or uptime claims are included.

## Next Step

`AGENT-006` added local validation in `scripts/check-agent-registry.mjs`.

`AGENT-007` added a protected owner/admin read-only readiness surface in `/admin` and `/settings`.

The next protocol-specific implementation slice is `AGENT-008`: propose a NANDA Index adapter and registration approval workflow. Do not start that before loop 30 launch review unless auth/Work/deployment proof is already unblocked and human approval is explicit.
