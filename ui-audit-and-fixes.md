# Personal OS UI 問題盤點與修復報告

**日期：** 2026-08-24
**依據：** `RPT-066` 簡化 SaaS UI 交接報告、`ARC-036` 簡化 SaaS 操作介面設計模式
**驗證方式：** 已在本機 `pnpm dev`（localhost:3000）以 Owner 帳號實際登入，用 Claude in Chrome 逐頁截圖比對，非僅讀原始碼推測
**設計參考：** [Payload CMS Admin Panel](https://payloadcms.com/docs/admin/overview)

## 1. 為什麼拿 Payload CMS 當參考

Payload 的 admin panel 是目前 headless CMS / 內部工具 UI 的代表性風格，幾個可以直接套用在 Personal OS 的原則：

- **極簡外殼**：左側窄 sidebar 只放 collection／功能入口，不塞狀態文字；頁面主體永遠是「一個標題 + 一個主要動作 + 一個工作區」。
- **List → Detail，不是卡片牆**：資料用可排序、可篩選的表格呈現，選一列才展開細節，不會把所有欄位一次攤在畫面上。
- **關聯資料用 Drawer（側邊彈窗）**：編輯關聯物件時不跳頁，而是疊一層 slide-over，做完就收起來，主畫面狀態不丟失。
- **克制的視覺語言**：接近黑白的中性色、單一強調色（primary action），邊框細、陰影淡，同層級卡片不互相搶視覺。
- **欄位分組用 Tabs**：一筆資料如果欄位多，會拆成 Content / Meta / Sidebar 幾個 tab，而不是一個長表單捲到底。

這份報告後面每一頁的「解決方法」都對應這幾條，而不是憑空加元件。

## 2. 全站共通問題（不只單一頁面）

| 問題 | 證據 | 解決方法 | 狀態 |
|---|---|---|---|
| CJK 文字逐字換行成一直行，整頁版面炸掉 | `/research` 首屏標題「研究工作台」「研究」與整段說明文字，每個字各佔一行，往下延伸超過一個螢幕高 | 用 flex/block 單欄堆疊取代巢狀 grid（`grid-cols-[180px_1fr]` 這類寫法在 sidebar + AI 助理面板夾擠出的窄欄位下會計算出接近 0 的欄寬）；已在 `/settings` 修好同一類 bug，`/research` 需要同一手法處理，見第 3.5 節 | `/settings` 已修，`/research` 待修 |
| 右側「AI 助理」面板固定佔用約 330px，所有頁面可用寬度被壓縮 | 每頁截圖右側都固定看到 AI 助理面板，中央內容欄實際只剩約 700px | 保留面板（這是產品定位的一部分），但頁面內容改用會隨欄寬正確收縮的排版（flex + `min-w-0` + `truncate`），不要假設有 1200px+ 可用寬度 | 本次已在調整過的頁面套用，其餘頁面沿用時要注意 |
| 工程／治理詞彙外洩到一般頁面（`externalRegisterable=false`、`L0_LOCAL_PROTOTYPE`、governance badge 牆） | `/ai-input` 舊版一次顯示 4 個狀態 badge＋來源索引表＋提案細節＋設定邊界卡；`/admin` 首屏直接印 `externalRegisterable=false` | 一般頁只留 1 個狀態 badge，其餘收進「查看詳細」彈窗（Dialog）；只有 `/admin` 允許保留技術細節，但也要摺疊，不做滿版攤開 | `/ai-input`、`/admin`、`/settings` 已修 |
| 同一頁面同時攤開「狀態摘要卡＋清單＋提案卡＋邊界說明」，首屏看起來像系統監控牆而非操作介面 | `/dashboard` 舊版首屏：3 張統計卡＋留言表單＋4 則待辦（各 3 行文字）＋提案卡（4 個 badge）＋邊界段落（2 段文字＋按鈕），總計遠超過 120 字門檻 | 拆成「一句話標題＋精簡數字列＋一行式待辦清單＋詳細彈窗」，把 RPT-066 §3 的 rule 落地 | `/dashboard` 已修 |

## 3. 逐頁現況與處理

### 3.1 `/dashboard`（今日）— 已完成

**問題**：首屏堆了 3 張統計卡、4 則待辦（每則 3 行文字：分類 badge、狀態 badge、signal、next step）、AI 提案卡（4 個 governance badge＋建立時間＋稽核預覽）、操作邊界卡（2 段長文字＋按鈕），首屏字數遠超過 RPT-066 訂的 120 字上限，使用者要往下滑很多才會看到真正能做的動作。

**解決方法**：改成「一句話標題（N 件事需要你看）→ 精簡三欄數字列（今日佇列／需要留意／模式）→ 一鍵展開的操作邊界彈窗 → 留言輸入框（保留 preset chip，但縮小成 pill）→ 待確認清單（改成 1-2-3 編號、單行標題＋單行 next step，狀態圖示與完整細節收進 info 彈窗）→ AI 摘要卡（結論一行＋詳細彈窗）」。整頁現在一個 1316px 視窗可以完整看完，不用捲動。

### 3.2 `/ai-input`（AI 工作桌）— 已完成，優先度最高

**問題**：這是 Gate A 的核心工作桌，但舊版在使用者看到輸入框之前，要先看完：4 個狀態 badge、標題、副標題、目前對話資訊、4 個指令按鈕，再往下是「來源索引表（4 個統計數字＋4 列來源）」「提案細節卡（含次決策、workflow 統計）」「設定與邊界卡（4 個 governance 標籤）」，接著才是 tab 列（每個 tab 還帶一行描述文字）與「示範資料模式」通知條，實際聊天輸入框要捲動約 650px 才會出現。

**解決方法**：
- 頂部壓成一列：標題＋一個模式 badge＋精簡三數字 InsightRail（審核／來源／脈絡）＋指令按鈕＋一個「查看詳細」彈窗（把原本整組來源索引表、提案細節卡、設定邊界卡收進去）。
- Tab 列拿掉每個 tab 底下的描述文字，改成純 icon＋label＋數字徽章的緊湊 pill，貼近 Payload 的 tab 樣式。
- 「示範資料模式」通知從整條滿版色塊改成置中小圓角 pill。
- 結果：輸入框與「整理思路／專案更新」等快速動作在首屏就看得到，不用捲動。
- 追加修復：「同步設定」分頁的 `SourceStructurePanelContent` 原本疊了三層幾乎一樣的標題／說明（分頁外層的「同步設定」標題、面板自己的「外部資料源同步設定」標題、表格元件自己的「來源設定與同步管理面板」標題），三段文字內容高度重複。已拿掉面板與表格各自的標題／說明文字，只留分頁外層的標題，面板本身只留一列精簡的操作列（管理邊界說明圖示＋已串接／待設定／需確認數字＋新增連線按鈕），表格元件的 `title`/`description` 也改成選填。

### 3.3 `/settings`（設定）— 已完成

**問題**：整體結構其實已經接近目標（index card＋table＋dl），主要問題是「目前狀態」區塊有一個排版 bug：`Email` 等欄位值在窄欄位下用 `break-words` 造成逐字元換行，看起來像亂碼；另外頭部有 3 個 badge、模組顯示區塊有兩段說明文字常駐畫面。

**解決方法**：
- 「目前狀態」改成 label 在上、值在下的單欄堆疊＋`truncate`＋`title` tooltip，徹底避開巢狀 grid 窄欄位計算錯誤的問題（見 3.5 節的根因分析）。
- 頭部 badge 精簡到 2 個，第 3 個收進「…」彈窗。
- 「模組顯示 rehearsal」的說明文字改成一個「說明」按鈕開彈窗，卡片本身只留一行摘要。

### 3.4 `/admin`（管理）— 已完成

**問題**：「下一步」欄位直接印出一整段沒有斷行、夾雜任務代號（`OWNEROS-002B`、`AUTH-005`）的長段落文字，把整個摘要區撐得極高；阻塞清單表格的 Signal／Next 欄位也是不截斷的長文字。

**解決方法**：長文字一律先截斷成單行（約 60-90 字），旁邊放「完整」或 info 圖示按鈕，點開才看到全文；阻塞清單表格欄位改成 `line-clamp-2` 加每列一個詳細彈窗。管理頁本來就允許保留技術細節（RPT-066 §4.9），這裡不是砍內容，而是把攤平的長文字改成可展開的層級。

### 3.5 `/settings` 排版 bug 的根因（給後續頁面參考）

用「180px 標籤欄＋`minmax(0,1fr)` 值欄」的巢狀 grid／flex，在「sidebar（245px）＋AI 助理面板（約330px）」夾擠出的窄內容區裡，瀏覽器有時會把值欄計算成接近 0 寬，中文字沒有空白可斷字，於是逐字換行甚至完全不可見。這不是單一頁的個案，`/research` 目前也是同一種症狀（見下方）。修法統一是：**別在窄欄位環境賭巢狀 grid 版寬計算，改用單欄堆疊或 `flex` + `min-w-0` + `truncate`**，這也剛好符合報告要的「mobile-like、一次只專注一個欄位」方向。

### 3.6 `/research`（研究）— 已完成

**問題**：這是本次盤點中最嚴重的個案。首屏標題「研究工作台」「研究」與整段導言文字，每個中文字各自佔一整行，從畫面頂端一路往下延伸超過一個螢幕高，完全無法閱讀，使用者要瘋狂往下捲動才可能看到真正的內容。此外整頁有嚴重的功能重複：頂部 4 個指令按鈕、「研究佇列」5 列清單、「來源證據」卡、「Upcoming CFP」卡，這四塊其實指向同樣幾件事（來源、事件、佇列），加上「研究 AI 提案」「紀錄／稽核」「設定／邊界」三個純文字說明區塊，首屏文字量遠超上限。

**解決方法**：
- 換掉造成逐字換行的 `grid-cols-[1fr_auto]` 標題容器，改用共用的 `AppHeader`（標題＋副標題），徹底移除該排版模式。
- 拿掉「原型資料／正式持久化待接／受保護工作區／對外登錄關閉」4 個常駐 badge 與「設定待完成」提示列——這些是治理用語，一般操作頁不需要。
- 指令按鈕從 4 個精簡到 2 個（新增研究、Readiness），另外 2 個（Full Research list、Source Evidence）本來就與下方「研究佇列」清單重複，直接刪除。
- 「研究狀態」的 3 個統計卡改成 `InsightRail` 精簡數字列。
- 「研究 AI 提案」（3 張純說明卡）、「紀錄／稽核」（3 列純說明文字）、「設定／邊界」（5 個 boundary chip）三個區塊全部移除——這些是不可操作的治理說明，不是使用者需要的功能。
- 「來源證據」重複卡片刪除（與佇列列表的 Source Evidence 重複）；「Upcoming CFP」保留但併入右欄，因為它顯示的截止倒數天數是佇列列表沒有的實際資訊。
- 結果：整頁從原本捲不完的治理牆，縮成「指令列＋3 個數字＋研究佇列清單＋近期議題／即將截止」，一個半螢幕內看完，已用瀏覽器截圖驗證排版正常、無逐字換行。

### 3.7 `/inbox`（收件匣）— 已完成

**問題**：整體已經是 Payload 風格的「queue／list」樣式，首屏字數與資訊密度控制得不錯。可再優化的地方：每張提案卡的「AI 判斷依據」「原文依據」是兩個各自獨立的內嵌展開按鈕，點開後直接把推理文字與原文證據列表往下撐開在卡片內，卡片越長越難掃視整份清單。

**解決方法**：把「AI 判斷依據」「原文依據」兩個內嵌展開合併成一個「查看依據」`DetailDrawer`，同時包含 AI 推理文字與原文證據列表；卡片本身維持原本的精簡列狀態，不再因為展開內容而變長。已用瀏覽器截圖驗證，含實際點開一張提案卡的依據彈窗，證據段落正確顯示。

### 3.8 `/work`（工作）— 已完成

**問題**：首屏堆了標題列、4 個狀態 badge、重複的第二個 h1（與 AppHeader 標題重複）、「manual-ops-handoff」證明步驟卡、5 個指令按鈕（新增決策／檔案／AI 提案／Records／設定），接著是「專案佇列／專案狀態／客戶邊界＋工作 AI 提案」3 欄卡片——而這 5 個指令按鈕其實跟下方真正的 tab 列（專案／檔案／工作 AI／紀錄／設定）功能完全重複，等於同一組導覽出現兩次；「專案佇列」清單也和「專案」tab 裡本來就有的專案清單重複；「專案狀態」（單一專案的詳細卡）則和「專案」tab 裡「Today's focus」的 ProjectFocusCard 重複。

**解決方法**：
- 拿掉重複的第二個 h1／說明文字，只留 `AppHeader`。
- 拿掉 4 個狀態 badge 與整張「manual-ops-handoff」證明步驟卡，內容併入詳細彈窗。
- 拿掉 5 個指令按鈕——tab 列本來就能做一樣的事，不需要兩份導覽。
- 拿掉「專案佇列」清單與「專案狀態」卡——都已經在「專案」tab 的內容裡有更完整的版本（Today's focus＋完整專案清單）。
- 「客戶邊界」checklist＋「工作 AI 提案」說明文字收進一個 `DetailDrawer`。
- 頂部改成一列 `InsightRail`（進行中／風險／客戶共享）＋一個查看詳細彈窗＋新增專案按鈕，維持在所有 tab 都可見；tab 本身的內容（專案／檔案／工作 AI／紀錄／設定）完全不動，因為那些已經是各自獨立、資訊分區清楚的頁面。
- 結果：已用瀏覽器截圖驗證，首屏乾淨且不再有雙重導覽，各 tab 切換正常、彈窗內容正確顯示。

### 3.9 `/company`（公司）— 已完成

**問題**：首屏 4 個狀態 badge（高風險策略模組／原型狀態／正式知識待核准／對外登錄關閉）、重複的第二個 h1（與 AppHeader 標題重複）、4 個指令按鈕（其中「新增決策」其實只是捲動到下方區塊，並非真的建立功能，屬於誤導性操作）、右側「設定檢查」摘要卡。往下還有「公司狀態」卡（重複顯示「設定檢查」的完整規則＋主要決策摘要）、「公司 AI 提案」說明卡、「紀錄／稽核」記錄卡、「設定／邊界」5 個 chip——這些全部是純說明性質的治理文字，沒有互動功能。

**解決方法**：
- 拿掉 4 個狀態 badge、重複的第二個 h1、4 個指令按鈕（含誤導性的「新增決策」按鈕）。
- 「公司狀態」「公司 AI 提案」「紀錄／稽核」「設定／邊界」「設定檢查」全部合併進一個「公司狀態與邊界」`DetailDrawer`，不再各自佔一張卡。
- 頂部改成 `InsightRail`（公司項目／高風險／P0）＋一個查看詳細彈窗，「公司 Lane」清單維持原樣作為唯一主要內容。
- 結果：已用瀏覽器截圖驗證，首屏只剩數字列＋清單，彈窗內容完整可讀。

### 3.10 `/agents`（AI 指令中心）— 已完成

**問題**：首屏是「4 個狀態 badge＋重複的第二個 h2（與 AppHeader 標題重複）＋2 個統計卡＋4 個 readiness chip＋4 條硬邊界 badge」，總共 12 個徽章/chip 堆在同一個區塊，才進入真正可操作的指令路由介面。

**解決方法**：
- 拿掉重複的第二個 h2／說明文字，只留 `AppHeader`。
- 12 個徽章/chip 全部收進一個「查看詳細」`DetailDrawer`。
- 頂部改成 `InsightRail`（指令數／群組數）＋查看詳細彈窗；下方「指令路由」選單、指令詳情、Owner instruction 輸入框、Dry-run／建立提案按鈕、提案審閱／dry-run 結果面板、CLI/HTTP 對齊、模組就緒總表全部維持原樣——這些是這頁真正的操作區，本來就已經是 Payload 風格的 list + 詳情 + 表格。
- 結果：已用瀏覽器截圖驗證，彈窗內容正確、指令路由與 dry-run 互動不受影響。

### 3.11 `/workflow`（自動化）— 已接近目標

**問題**：這頁目前是本次盤點中最接近 Payload 風格的頁面：左側 Agent 狀態清單、中間流程圖、下方「規則列表／事件日誌」tab，資訊分區清楚，沒有明顯的文字牆或治理用語外洩。

**解決方法**：暫不需大改，可視情況把「規則列表」的規則卡再精簡一點（目前每條規則已經是條件式的緊湊呈現，優先度較低）。

### 3.12 `/`（首頁）與 `/login`（登入頁）— 已完成

**問題**：這兩頁是未登入訪客看到的第一畫面，卻是全站文字密度最高的頁面。首頁 (`/`) 有一段落式的產品介紹＋3 個 CTA 按鈕＋3 條打勾說明句、右側「Launch boundary map」卡（Public/Login/Private 3 欄說明＋「Loop 8」badge＋「Auth readiness JSON」連結）、下方再兩張卡「Public exposure boundary」（3 列，每列一段長描述句）與「Launch readiness」（3 列，每列一段長描述句）——這些都是內部 launch-readiness／治理狀態，不該出現在公開首頁。登入頁 (`/login`) 除了正常的 Email 驗證表單外，還有整張「Owner access readiness」卡（Ready/Blocked badge、每個驗證方式的 status/nextAction/指令 code block，例如 `PERSONAL_OS_AUTH_MODE=mock ... pnpm dev`），把開發環境設定細節攤在訪客看得到的登入畫面上。

**解決方法**：
- `/`：拿掉「Launch boundary map」「Public exposure boundary」「Launch readiness」三張治理卡與 3 條打勾說明句，只留「標題＋一句話說明＋一個登入按鈕」，CTA 從 3 個減到 1 個（其餘 owner settings／admin console 入口，登入後從側邊欄本來就找得到，不需要在公開首頁重複曝光）。
- `/login`：整張「Owner access readiness」卡（含 command code block、Ready/Blocked badge、每個管道的簽名細節）全部移除；表單本身從左右兩欄改成單欄置中卡片，狀態提示文字（Email 六碼驗證的次要說明句）精簡掉，只保留使用者實際需要按的表單欄位與按鈕。
- 結果：首頁與登入頁都已用瀏覽器截圖驗證，首屏乾淨，不再外洩 launch-readiness／環境變數等內部治理資訊。

## 4. 已完成 vs 待處理總表

| 頁面 | 狀態 | 本次改動摘要 |
|---|---|---|
| `/dashboard` | 已完成並已用瀏覽器驗證 | 首屏字數大幅下降、待辦改一行式＋詳細彈窗 |
| `/ai-input` | 已完成並已用瀏覽器驗證 | 頂部治理牆收進彈窗、tab 列精簡、首屏可直接看到輸入框；同步設定分頁的三層重複標題已合併 |
| `/settings` | 已完成並已用瀏覽器驗證 | 修正欄位換行/消失 bug、badge 收斂 |
| `/admin` | 已完成並已用瀏覽器驗證 | 長文字截斷＋詳細彈窗、阻塞清單可展開 |
| `/research` | 已完成並已用瀏覽器驗證 | 修正逐字換行版面 bug、指令按鈕/佇列/來源卡三重重複合併成一份清單、治理說明區塊全數移除 |
| `/`、`/login` | 已完成並已用瀏覽器驗證 | 移除治理／readiness 卡牆與環境變數細節，首屏只留標題＋說明＋登入 |
| `/inbox` | 已完成並已用瀏覽器驗證 | 提案卡的判斷依據／原文依據合併成一個彈窗 |
| `/work` | 已完成並已用瀏覽器驗證 | 拿掉與 tab 列重複的指令按鈕、拿掉與「專案」tab 重複的佇列/狀態卡，邊界說明收進彈窗 |
| `/company` | 已完成並已用瀏覽器驗證 | 4 badge/重複標題/4 指令按鈕移除，5 個純說明區塊合併成一個彈窗 |
| `/agents` | 已完成並已用瀏覽器驗證 | 12 個徽章/chip 與重複標題收進一個彈窗，指令路由與 dry-run 操作不變 |
| `/workflow` | 已接近目標，暫不需大改 | 見 3.11 |

全站 11 個路由（含首頁與登入頁）目前都已完成本輪簡化並在瀏覽器上驗證過畫面，僅 `/workflow` 原本就已接近目標、暫不需大改。

## 5. 共用元件（本次新增）

- `src/components/owneros/detail-drawer.tsx`：所有「查看詳細」彈窗共用的 Dialog 包裝，之後每一頁要做同樣的收斂都直接複用這個元件。
- `src/components/owneros/insight-rail.tsx`：精簡三欄數字列，取代原本一次攤開好幾張統計卡的做法。

兩者皆已通過 `tsc --noEmit` 型別檢查，且在本機 dev server 上實際渲染驗證過。

## 6. 後續建議

本輪列出的問題都已處理完成。之後若要再往下打磨，可以考慮：

- `/workflow` 的「規則列表」規則卡可以再精簡一點，但目前優先度低，現況已可用。
- 隨著更多頁面加入 `DetailDrawer`／`InsightRail` 這兩個共用元件，建議定期檢查是否有新頁面又不小心疊出「頁面標題 + 元件自己的標題」這種重複標題模式（本次在 `/research`、`/ai-input` 同步設定、`/work`、`/company`、`/agents` 都抓到同一種 bug），這類重複多半是元件被放進已經有 `AppHeader` 的頁面時忘記拿掉元件自帶的標題。

## 7. 2026-08-24 追加：Owner 針對 `/ai-input` 的五點直接回饋（詳見 `RES-028`）

**來源：** Owner（Oliver）針對本報告完成後的最新 `/ai-input` 畫面提供五張截圖與五點直接回饋，並已用 Claude in Chrome 在 `localhost:3000` 現場核對（示範／正式模式、快速擷取彈窗、`/settings`、`/ai-input` 側欄皆已實際點開截圖，非僅憑截圖推測）。完整分析、資料模型與 backlog 見新文件 `docs/07_research-and-design/RES-028_ai-work-desktop-chat-project-multiagent-and-folder-library-convergence-research.md`，此處只留追蹤用摘要。

| # | Owner 原話摘要 | 現場核對結果 | 對應 task id（見 `RES-028` §8） |
|---|---|---|---|
| 1 | 「這兩個要合併」 | 正式/示範狀態 badge 在 `/ai-input` 頂部同時出現兩次（右上角一次、標題列下方再一次）；「AI 對話」分頁標籤與「AI 工作桌」標題兩層命名疊在一起，第一次看容易誤解成兩個不同頁面 | `AIWORKDESK-001` |
| 2 | 「快速擷取點旁邊應該要可以關掉」 | 已在 `/dashboard` 現場點開「快速擷取 ⌘K」彈窗：標題列只有文字，沒有可點擊的 ✕，只有左下角「Esc 關閉」文字提示 | `AIWORKDESK-002`（`AIINPUT-CAPTURE-003` 的可收合擷取面板仍是後續大改，兩者不衝突） |
| 3 | 對齊 Codex 設計重做來源／檔案／多聊天管理介面，並要有 AI 自由交流區＋手動邀請多個 AI 加入聊天 | 現有 `RES-014` §3.4 只做「多代理協作（連結）」唯讀連結；`PLN-067` Stage 6（`OWNEROS-006`）仍 `BLOCKED_DECISION`。本次記錄 owner 對「可見的 AI 交流空間＋手動邀請」的直接指示，但**不**視為完整解除 Stage 6 封鎖（寫入/提案能力仍需 `RPT-062 §3.5` Q3/Q4 答覆） | `AIWORKDESK-007`、`AIWORKDESK-008` |
| 4 | 側欄要能收縮，最下面要有 ClickUp 風格成員小卡，使用者設定／系統設定要分開頁面 | 已讀 `app-sidebar.tsx` 全文：目前 `w-56` 固定寬度、無收合控制，footer 只有文字提示＋一個示範/正式 badge，沒有成員卡；`/settings` 現場核對混雜個人欄位（Email/語言）與系統欄位（成員/角色/AI分享/模組/人工設定） | `AIWORKDESK-009`、`AIWORKDESK-010`、`AIWORKDESK-011`（新增路由 `/settings/system`） |
| 5 | 參考脈絡頁面充斥 demo 資料、正式版無法新增、與聊天室引用資料夾流程衝突；要合併進聊天室並對齊 Claude 的 chat/project 管理；檔案庫/媒體庫之外要多一個不依賴本機的資料夾管理項目 | 已在正式模式現場核對 `/ai-input` → 參考脈絡：已加入 0／來源池 0／可引用 0，完全是死路，與 owner 描述一致 | `AIWORKDESK-003`〜`006`（`ChatProject` 容器＋新 `資料夾庫` 分頁，取代 `RES-014`/`RES-015` 原本的 `folderId`／單純逐則 modal 設計，詳見 `RES-028` §7 對照表） |

**狀態：** 全部 `AIWORKDESK-0xx` 目前為 `TODO`，尚未寫入 `PLN-060`/`PLN-061`/`tasks.md`（依 `AGENTS.md` §14，下一輪撿起本項目時需補上，避免手動編輯這幾份大檔案時與正在跑的 10 分鐘 Codex 迴圈衝突）。已交付 owner 一份對應的互動式 HTML mockup 供先行確認方向，再進入實作。
